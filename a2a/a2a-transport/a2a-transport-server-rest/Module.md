# Module a2a-transport-server-rest

HTTP+JSON/REST server transport implementation for A2A protocol. Implements the ServerTransport interface using Ktor HTTP server to expose RESTful endpoints for agent communication.
